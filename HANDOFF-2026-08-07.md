# HANDOFF — 2026-08-07

> 接手這個 session 的工作用。根目錄那份舊的 `HANDOFF.md` 是 **2026-06 agent-os 初期**寫的，
> 裡面第 3 節那串「需要先跟用戶釐清的問題」**早就都答完並實作了**（13→16 角色、knowledge/skills 層、
> 設定頁都已上線），拿它當現況會誤導。**現況權威說明看 `CLAUDE.md`**，這份只記「今天動了什麼、還沒做完什麼」。

---

## 0. 現在在哪

| | branch | HEAD | 推了嗎 |
|---|---|---|---|
| `yagent`（後端） | `feature/0807/multi-room` | `797df06` | ❌ 未推 |
| `web`（子模組） | `feature/0807/multi-room` | `b97dbdf` | ❌ 未推 |
| `yagent` main | `main` | `2461814` | ✅ 已與 origin 同步 |
| `web` master | `master` | `9c30292` | ✅ 已與 origin 同步 |

未追蹤檔案：`talk/`（COSCUP 簡報，見 §3）、`note.md`（報價手測 prompt）、`.DS_Store`。

**驗證狀態**：`npx tsc --noEmit` 綠、`npm --prefix web run build` 綠。兩者在 commit 前都跑過。

---

## 1. 今天做完的三件事

### (a) 把 `feature/geo` 併回 main 並推上去

之前的斷點是「前端有 GEO 面板、後端沒有 `/api/geo`」——因為後端那半留在 `feature/geo` 沒併。
已合併（`cab2404`），衝突只有 `src/config.ts` 一處（`EAIERP_*` 與 `GEO_*` 兩段各自 append，兩邊都留）。

推之前遇到遠端領先一筆（Yale 在 GitHub 網頁上刪掉 `__todo_0530:read_this.md`），用 **merge 不用 rebase**
（這條線有三個 merge commit，rebase 會壓平拓撲）合進來後才推成功。

現在 `main` 上 GEO 前後端在同一條線上了。⚠️ **`POST /api/geo` 會真的打多個 AI engine，是付費操作**，
不要為了「測一下」隨手打。`GET /api/geo` 只列歷史報告，免費。

### (b) 兩包收尾的東西

- `d8115c0` — tsx 跑 source 時不再從 API port 端出 `web/dist`。那是 production bundle，
  `NEXT_PUBLIC_API_BASE` 已 inline 成線上 backend，從那個 port 打開 = 舊 UI 靜靜連到 prod，
  打一句話就是真的花錢。現在回 421 導去 `:3000`。
- `9c30292`（web）— 「停止這一輪」按鈕。後端 abort（`src/abort.ts`）在 `f5d2a7f` 就有了，
  這次是前端補上入口，順手修掉「WS 重連後沒重抓 session list，header 綠燈但左側永遠空」的 bug。

### (c) 多間會議室（本次主要功能，在 `feature/0807/multi-room`）

後端本來就一房一檔（`.rooms/<id>.json`），缺的只是入口與幾個「反正只有一間」的假設：

- `createRoom` / `deleteRoom`。id 兼作檔名與 sessionKey 的一段（gateway 用 `/^room:([^:]+)$/` 比對），
  限制在 `[a-z0-9-]`；中文房名 slug 後是空的 → 退回 `room` 加序號，同名碰撞一併解決。
- **刪房會連帶刪掉成員在該房的 sub-session**（`room:<id>::<roleId>`）——不刪會變 session 清單的孤兒，
  且同 id 的新房會撿到別人的記憶。
- `addParticipant` / `removeParticipant` / `runRoomMessage` 原本查無房間時 fallback 到 `main`，
  多間之後這是錯的（訊息被送進使用者沒在看的房間）→ 改成 404 / 丟棄。
- `ensureDefaultRoom` 改成「一間都沒有」才播種，否則刪掉 `main` 下次重啟又長回來。
- **CORS `Allow-Methods` 補上 `DELETE`** — 這是 UI 實測才發現的：preflight 過了但實際 DELETE 被瀏覽器
  擋成 `net::ERR_FAILED`。**curl 不吃 CORS，所以 API 層完全測不到這個 bug。**

前端把寫死的 `rooms[0]` 換成 `activeRoomId`，側欄 Room channels 變房間清單（＋新會議室、
開會中有綠點），標題列放房名與刪除鈕，空狀態改成建立 CTA。

`CLAUDE.md` 的 room 段落與 REST 清單已同步更新。

---

## 2. 立刻要決定的事

1. **`feature/0807/multi-room` 要推嗎？要合回 main 嗎？** 目前只在本機。合併前記得
   **web 子模組要先推**，否則 main 記錄的 submodule 指標會指向遠端不存在的 commit。
2. **`mobile/` 還是沒跟上**。欠的清單已經很長：`dispatch:*` / `cost:*` / `budget:*` / `delegate:*` /
   `room:*` 事件、`roleId`、role/usage/tools/agents/threads-sources/monitor/rooms 端點、
   `threadsSource` 欄位，**外加今天的 abort 與 GEO、多間會議室**。下次碰 mobile 要一次補齊。
3. **七條 ahead-0 的舊 feature 分支可以刪**（`swpm-agent` / `quote-agent` / `room-channels` /
   `yale-agent` / `flow-bot` / `delegate` / `memory`），功能都在 main 裡了。`feature/geo` 也已併入。

---

## 3. COSCUP 簡報（`talk/coscup-2026-yagent.html`，未 commit）

單一 HTML、38 頁、不吃網路。鍵位：`←→` 換頁、`S` 講稿、`T` 計時、`R` 歸零、`O` 總覽、`F` 全螢幕。
右下角有「實際計時 vs 該頁目標」，超過 45 秒變紅。

**題目**：AI Agent 探索：多角色任務處理 Agent（初階場）
**時間**：15 主講 / 5 `yagent-hand` / 5 demo / 5 QA

主講 15 分：名詞 3.5 → 動機 2 → 怎麼開始 1.5 → building blocks 4.5 → 對外串接 2.5。

**Yale 已定調的方向**（別自己改）：
- 動機三條：想搞懂內部原理、當這場分享的教材、要接自家系統＋控成本
- Demo 是**現場 live**
- **商業內容只講技術，一句帶過** — ERP 只說「一套自己寫的 ERP」，不放 company-plan、定價邏輯、客戶名
- 指定要放：成本 dashboard（p23）、prompt injection（p24）、「vibe 之餘要理解運作方式」、「怎麼自己動手做」

**`yagent-hand`（`../yagent-hand`）**是第二個 demo 專案：純 Node、無框架、~300 行、2 個 commit，
`server/agent/agent.js` 79 行就是手寫的 agent loop，工具是行事曆情境（查空檔／建行程／設提醒）＋
一個手寫的 skill 機制。簡報 p29–34 是這段。⚠️ 那個 repo 的 `CLAUDE.md` 是**過期的**
（還在講 `index.js` / `src/llm.js`，實際已經是 `app.js` + `server/`），而且 `.env` **沒有被 gitignore**。

**還沒做完**：
- 封面掛「Yale · ElementAI」，要不要改講者名／加場次資訊
- **demo 那四段（會議室 → Yale 分身 → SWPM 派工 → 報價）還沒實跑計時**。
  會議室那段主持人點名 + 三個角色發言，很可能超過 90 秒 → 超了就砍成兩個角色。
  跑一次要花真的 API 錢，**動手前先問 Yale**。

---

## 4. 這個 repo 的地雷（會重複踩的）

- **`:3000` 那個 next-server 接的是線上 backend**（`yagent-api.zeabur.app`），不是 localhost。
  在那上面看不到任何本地改動。要驗本地改動請用 `.claude/launch.json` 的 `web` 設定
  （preview_start `{name:"web"}`，跑在 `:3005`，吃 `.env.development` → `localhost:3001`）。
- **`:3001` 常常已經有 Yale 自己的 `tsx watch` 在跑**。它會 hot-reload 你的改動，所以直接對它打 API
  就能驗後端；但別 kill 它。
- **CORS 只有瀏覽器會擋**。curl 全綠不代表 UI 能用（今天的 DELETE 就是這樣漏掉的）。
- **Zeabur 檔案系統是 ephemeral**，`.sessions` / `.memory` / `.usage` / `.rooms` 重新部署就沒了。
  掛 volume 這件事**還沒做**。
- **`codex.ts` 從來沒對真的 `codex` binary 跑過。**
- Claude 計費：`claude -p` 只有在 `ANTHROPIC_API_KEY` **未設**時才走訂閱，否則靜靜走 metered API。

---

## 5. 給接手者的第一步

先 `git status` 確認還在 `feature/0807/multi-room`，然後問 Yale §2 的第 1 題（要不要推／合）。
如果是要繼續做簡報，直接開 `talk/coscup-2026-yagent.html`，重點是 §3 最後那兩個未完項。
